# Git指令

```
//在github新建仓库，在本地目录下gitbash + git init + git add -A + git commit之后，连接远程库并上传
git remote add origin git@github.com:VictoranusQ/sadgasdg.git（根据新建库提示来）
git branch -M main
git push -u origin main
```



